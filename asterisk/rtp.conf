;==============================================================
; rtp.conf  —  音声(RTP)が使うポート範囲
; host ネットワークで動かすのでこの範囲がそのまま使われます。
;==============================================================
[general]
rtpstart=10000
rtpend=20000
